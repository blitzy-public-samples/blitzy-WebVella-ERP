@*
    Design-time preview view for PcApprovalRequestList component.
    Renders a static placeholder representation in the page builder.
    This view does not load runtime data due to WebComponent limitations.
*@
<div class="p-2">
	<h4><i class="fas fa-list-alt"></i> Approval Request List</h4>
	<p>Displays pending approval requests for review. WebComponents fail to load properly so the text only design.</p>
</div>
@* Design-time limitation: WebComponents do not render properly in page builder preview mode. Reference: https://github.com/ionic-team/stencil/issues/793 *@
