@*
    Design.cshtml - Page builder preview for PcApprovalWorkflowConfig component
    
    Displays a static representation of the workflow configuration component
    for visual identification during page design.
    
    STORY-008: UI components must support Design render mode
    Design views provide page builder preview with minimal, stateless HTML
*@
<div class="p-2">
	<h4><i class="fas fa-cogs"></i> Approval Workflow Configuration</h4>
	<p>Manage approval workflow definitions, steps, and rules</p>
</div>
@* Design view uses text-only representation for page builder compatibility *@
